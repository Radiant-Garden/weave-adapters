# Build output
/bin/
/dist/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Host-built Docker artifact (COPYed into the image)
weave-adapter-dhcp-windows-linux-arm64

# Test & coverage artifacts
*.test
*.out
coverage.txt
coverage.html
profile.cov
*.prof

# Go workspace (not used yet; keep local if introduced)
go.work
go.work.sum

# Dependency vendoring (we use the module cache, not vendor/)
/vendor/

#LLM
/.claude
CLAUDE.md

# Editor / IDE
/.idea/
.vscode/
*.swp
*.swo
*~

# OS cruft
.DS_Store
Thumbs.db

# Local env / secrets (config precedence supports *_FILE mounts; never commit)
.env
.env.local
*.local.yaml
*.pem
*.key
